PORT=5000
MONGODB_URI=mongodb://127.0.0.1:27017/dentalpay
CLIENT_ORIGIN=http://localhost:5173

# Local development only. Group 1 must replace mock auth with verified JWT auth.
AUTH_MODE=mock
NODE_ENV=development
