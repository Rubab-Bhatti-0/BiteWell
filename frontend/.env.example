# Leave empty during local development because Vite proxies /api and /uploads.
VITE_API_URL=

# For a separately deployed backend, use its origin without a trailing slash:
# VITE_API_URL=https://api.example.com
